plugins {
    // Spring Boot 플러그인: 실행 가능 JAR(BootJar) 생성, 의존성 관리 등 부트 애플리케이션 빌드 지원
    // 예: ./gradlew :app:bootJar → BOOT-INF/lib에 모든 의존성을 포함한 fat JAR 생성,
    //     ./gradlew :app:bootRun → 애플리케이션을 로컬에서 바로 실행
    id 'org.springframework.boot'
    // Jib: Docker 데몬 없이 컨테이너 이미지를 빌드·푸시하는 Google 제공 플러그인
    // 예: ./gradlew jib → Dockerfile 없이 Docker Hub에 이미지를 빌드·푸시,
    //     ./gradlew jibDockerBuild → 로컬 Docker 데몬에 이미지를 빌드
    id "com.google.cloud.tools.jib" version "3.4.0"
    // Download: 외부 파일을 Gradle 태스크로 다운로드하는 플러그인
    // 예: download.run { src 'https://...' dest 'glowroot.jar' }로 Glowroot 에이전트 자동 다운로드
    id "de.undercouch.download" version "5.3.0"
}

// 루트 프로젝트의 version·group을 상속받아 일관된 아티팩트 좌표 유지
// 예: rootProject.version이 "1.7.0"이면 app 모듈도 동일하게 "1.7.0",
//     Docker 태그, JAR 파일명 등이 모두 이 버전을 사용
version = rootProject.version
group = rootProject.group


dependencies {

    // ────────────────────────────────
    // 프로젝트 모듈
    // 모든 impl 모듈을 조립하여 하나의 실행 가능 애플리케이션으로 구성
    // 예: Spring Boot가 기동될 때 각 모듈의 @Component, @Service, @Repository를W
    //     컴포넌트 스캔으로 자동 발견하여 Bean으로 등록
    // ────────────────────────────────
    // 인증 구현 모듈: JWT 발급·검증, OAuth2 소셜 로그인 처리
    // 예: AuthServiceImpl, JwtTokenProvider 등이 이 모듈에 위치
    implementation project(':auth-impl')
    // AWS 구현 모듈: S3 파일 업로드·다운로드 처리
    // 예: S3ServiceImpl, PresignedUrlGenerator 등이 이 모듈에 위치
    implementation project(':aws-impl')
    // 퀴즈 구현 모듈: 퀴즈 CRUD, 출제·채점 로직
    // 예: QuizServiceImpl, QuizRepository 등이 이 모듈에 위치
    implementation project(':quiz-impl')
    // 퀴즈 히스토리 구현 모듈: 퀴즈 풀이 기록 CRUD
    implementation project(':quiz-history-impl')
    // 유틸리티 구현 모듈: 범용 헬퍼 기능
    // 예: HealthCheckController 등이 이 모듈에 위치
    implementation project(':util-impl')
    // AI 모듈: Google Gemini 기반 AI 기능
    // 예: AiService에서 ChatClient를 통해 Gemini API를 호출
    implementation project(':ai-impl')
    // 공통 모듈: 전역 예외 처리, 공통 응답 DTO
    // 예: GlobalExceptionHandler, ApiResponse<T>, BaseEntity 등이 이 모듈에 위치
    implementation project(':global')
    implementation project(':board-impl')


    // ────────────────────────────────
    // 스프링부트 Web & JPA
    // ────────────────────────────────
    // Spring MVC 기반 웹 애플리케이션 구성
    // 예: 내장 Tomcat이 기동되고, DispatcherServlet이 모든 HTTP 요청을 라우팅
    implementation "org.springframework.boot:spring-boot-starter-web"
    // Spring Data JPA: JPA 기반 리포지토리 자동 생성, 엔티티 매핑 지원
    // 예: spring.jpa.hibernate.ddl-auto=validate 설정으로 엔티티와 DB 스키마 일치 여부를 검증
    implementation "org.springframework.boot:spring-boot-starter-data-jpa"
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
    // Prometheus 메트릭 엔드포인트 지원 (/actuator/prometheus)
    runtimeOnly 'io.micrometer:micrometer-registry-prometheus'
    // H2: CI 환경에서 OpenAPI spec 추출 시 인메모리 DB로 사용
    runtimeOnly 'com.h2database:h2'
    // Configuration Processor: @ConfigurationProperties 클래스의 메타데이터를 자동 생성
    // 예: @ConfigurationProperties(prefix = "app.jwt")로 선언하면
    //     IDE에서 application.yml의 app.jwt.* 속성을 자동완성·타입 검증 가능
    annotationProcessor 'org.springframework.boot:spring-boot-configuration-processor'
}

springBoot {
    // buildInfo(): META-INF/build-info.properties 파일을 생성
    // 예: 빌드 후 파일에 build.version=1.7.0, build.time=2024-01-01T00:00:00Z 등이 기록되고,
    //     Actuator /info 엔드포인트에서 {"build":{"version":"1.7.0",...}} 형태로 노출됨
    buildInfo()
}

// 로컬 실행 시 Glowroot 에이전트 적용
// 예: ./gradlew :app:bootRun → http://localhost:4000 에서 Glowroot UI 접근 가능
bootRun {
    def glowrootJar = project.file("./glowroot/glowroot.jar")
    if (glowrootJar.exists()) {
        jvmArgs = [
            "-javaagent:${glowrootJar.absolutePath}",
            "--add-opens=java.base/java.lang=ALL-UNNAMED",
            "--add-opens=java.base/java.util=ALL-UNNAMED",
            "-Dglowroot.data.dir=${project.file('./glowroot/data').absolutePath}"
        ]
    }
}

// Jib 설정: jib 태스크 요청 시에만 평가하여 CI에서 프로퍼티 없이도 빌드 가능
if (gradle.startParameter.taskNames.any { it.contains("jib") }) {
    jib {
        from {
            // 베이스 이미지: Eclipse Temurin JRE 21 (벤더 중립 OpenJDK 배포판)
            // 예: 최종 컨테이너는 eclipse-temurin:21-jre 위에 앱 JAR + 의존성이 레이어로 추가됨
            //     JRE만 포함하여 이미지 크기를 최소화하고, 클라우드 벤더에 종속되지 않음
            image = "qasker/q-asker-api-server-base:latest"
            platforms {
                platform { os = "linux"; architecture = "arm64" }
            }
        }
        to {
            // 푸시할 Docker 이미지 이름: {DOCKER_ID}/{DOCKER_IMAGE_NAME}
            // 예: DOCKER_ID=myuser, DOCKER_IMAGE_NAME=qasker → myuser/qasker
            image = "${project.property("DOCKER_ID")}/${project.property("DOCKER_IMAGE_NAME")}"
            // latest 태그 + 프로젝트 버전 태그를 동시에 푸시
            // 예: ["latest", "1.7.0"] → myuser/qasker:latest, myuser/qasker:1.7.0 두 태그 생성
            tags = ["latest", rootProject.version.toString()]
            auth {
                // Docker Hub 인증 정보 (gradle.properties 또는 -P 옵션으로 전달)
                // 예: -PDOCKER_ID=myuser -PDOCKER_PASSWORD=secret 으로 빌드 시 전달
                username = project.property("DOCKER_ID")
                password = project.property("DOCKER_PASSWORD")
            }
        }
        extraDirectories {
            paths {
                path {
                    // 로컬 ./glowroot 디렉토리를 컨테이너의 /app/glowroot 경로에 복사
                    // 예: app/glowroot/glowroot.jar → 컨테이너 /app/glowroot/glowroot.jar
                    setFrom(file("./glowroot").toPath())
                    into = "/app/glowroot"
                }
            }
        }
        container {
            jvmFlags = baseJvmFlags() + glowrootJvmFlags()
            // 서비스 포트(8080) + Actuator 전용 포트(9090) + Glowroot UI 포트(4000)
            ports = ["8080", "9090", "4000"]
            user = "appuser"
            environment = [
                "HOME": "/home/appuser"
            ]
        }
    }
}

// ── JVM 플래그 헬퍼 함수 ──

// JVM 기본 설정: 힙 메모리 등 애플리케이션 런타임 옵션
def baseJvmFlags() {
    def jvmHeapSize = project.property("JVM_HEAP_SIZE")
    def gcType = project.property("JVM_GC_TYPE")
    def maxGcPauseMillis = project.property("JVM_MAX_GC_PAUSE_MILLIS")
    return [
        // 초기 힙과 최대 힙을 동일하게 설정하여 GC로 인한 힙 리사이징 오버헤드를 방지
        "-Xms${jvmHeapSize}", "-Xmx${jvmHeapSize}",
        // GC 알고리즘
        "-XX:+Use${gcType}",
        // GC pause 목표 (ms)
        "-XX:MaxGCPauseMillis=${maxGcPauseMillis}",
        // OOM 발생 시 덤프
        "-XX:+HeapDumpOnOutOfMemoryError",
        "-XX:HeapDumpPath=/var/log/heapdump.hprof"
    ]
}

// Glowroot APM 설정: 에이전트 어태치, 데이터 디렉토리 지정
def glowrootJvmFlags() {
    // 에이전트 JAR 존재 여부 검증
    // 예: app/glowroot/glowroot.jar 파일이 없으면 빌드가 즉시 실패하여
    //     에이전트 누락된 채로 배포되는 상황을 방지
    def glowrootJar = project.file("./glowroot/glowroot.jar")
    if (!glowrootJar.exists()) {
        throw new GradleException("Glowroot agent file not found: ${glowrootJar.absolutePath}")
    }

    return [
        // Java 에이전트를 JVM에 어태치하여 성능 모니터링 활성화
        // 예: -javaagent로 로드되면 바이트코드 인스트루멘테이션으로 트랜잭션 추적,
        //     SQL 쿼리, HTTP 요청, 에러 등을 자동 수집하고 내장 Web UI(포트 4000)에서 조회 가능
        "-javaagent:/app/glowroot/glowroot.jar",

        // Java 16+ 모듈 시스템에서 Glowroot이 내부 API에 접근할 수 있도록 허용
        "--add-opens=java.base/java.lang=ALL-UNNAMED",
        "--add-opens=java.base/java.util=ALL-UNNAMED",

        // Glowroot 데이터 디렉토리 지정 (트랜잭션 기록, 설정 파일 등 저장)
        "-Dglowroot.data.dir=/app/glowroot/data"
    ]
}
