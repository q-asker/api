# 쿼리 튜닝 하네스 — 실행 런북

콜드 스타트부터 대시보드 확인까지 **순서대로 따라 실행**하는 절차서. "무엇을·왜"는 `README.md`,
설계 원리는 `../../docs/query-tuning/STEP1~4` 참조. 이 문서는 **"이 순서대로 치면 된다"**만 담는다.

## 컨벤션 (고정)

| 레벨 | 컨테이너 | 호스트 포트 | 규모(problem) |
|---|---|--:|--:|
| x1 | `local-mysql-x1` | 3307 | 71,244 (원본) |
| x10 | `local-mysql-x10` | 3308 | ~726k |
| x100 | `local-mysql-x100` | 3309 | ~7.28M (분석 대상, 대시보드가 읽음) |

- 전부 `root`/`password`, DB `qaskerdb`. 모두 `127.0.0.1` 바인딩(공개 금지) + `local_local-monitoring` 네트워크.
- 명령은 모두 `api/` 에서 실행한다.

---

## 0. 사전 준비 (최초 1회)

```bash
# PLG 스택(Grafana·Prometheus·Loki + local_local-monitoring 네트워크) 기동
cd ../plg-stack && docker compose up -d && cd ../api
docker network ls | grep local_local-monitoring   # 네트워크 존재 확인(provision-level 이 여기 붙는다)

# jq 설치 확인(로드젠·요약에 사용)
command -v jq || brew install jq

# OCI CLI + BACKUP_READER 프로파일(2단계 restore-x1 --latest 가 마스킹본을 내려받는다)
command -v oci && grep -q BACKUP_READER ~/.oci/config
```

- **Grafana**: http://localhost:3000 — 로그인 `admin` / `inha-cc-01`.
  (기본값이 안 먹으면: `docker exec local-grafana grafana cli admin reset-admin-password inha-cc-01`)
- MySQL 데이터소스(`qasker-mysql-local` → `host.docker.internal:3309`)는 **프로비저닝 자동 등록**이라 손댈 것 없음.

---

## 1. 컨테이너 생성 (x1/x10/x100)

빈 컨테이너를 prod-matched config(버퍼풀 4G·O_DIRECT 등)로 만든다. 데이터는 2·3단계가 채운다.

```bash
bash scripts/query-tuning/provision-level.sh local-mysql-x1   3307
bash scripts/query-tuning/provision-level.sh local-mysql-x10  3308
bash scripts/query-tuning/provision-level.sh local-mysql-x100 3309
# 기존 볼륨 보존 재생성: 3번째 인자로 볼륨명 (docker volume ls / inspect 로 확인)
```

> ⚠️ 3레벨 모두 버퍼풀 4G라 동시 상주 시 RAM 압박. 스윕은 한 번에 한 레벨만 띄운다(run.sh 이 자동 관리).

---

## 2. x1 채움 (OCI 마스킹본 → `local-mysql-x1` + 정합성 검증)

**책임 분리**: 마스킹 덤프 **생성은 plg-stack**(masked-export), **다운로드·복원·검증은 api**.

```bash
# 무인자 = 최신 마스킹본 자동 다운로드(내부에서 download-masked --latest 위임) + 복원 + 정합 검증 + top-up
bash scripts/query-tuning/restore-x1.sh
# 이미 받아둔 덤프가 있으면 경로 지정: restore-x1.sh /path/masked.sql.gz
```

> 두 스크립트는 **독립**(다운로드 / 복원 분리). OCI 접근은 `BACKUP_READER` 프로파일(`~/.oci/config`). 마스킹본이라 원본 PII 미도달.
>
> **x1 베이스 top-up**(`seed-x1-base.sql`): 복원·검증 뒤 `restore-x1.sh` 가 원본이 적은 소형 테이블
> (`board`·`feedback_board`·`quiz_folder`·`reply`)을 **x1 에서 100행으로 맞춘다**(부족분만 합성, `user`·`board` FK 재사용).
> 이 100행 base 가 §3 ×배수의 씨앗 → x100 에서 100×100=**10,000**(FLOOR 충족). 오프셋을 `< @base` 로 둬 복제 대상에 포함된다.

---

## 3. 배수 시딩 (초기화+복원+복제 한 방)

`seed-scale.sh` 가 **대상 볼륨 fresh 재생성 → x1 덤프 복원 → 배수 복제 → 검증**을 한 커맨드로 한다.
**재실행해도 매번 fresh** x\<배수\>(누적·중복키 없음). 배수만 바꾸면 어떤 스케일도 재현.
전 도메인 테이블이 ×배수 복제된다(`quiz_folder`·`reply` 포함 — 이전엔 빠져 있던 걸 편입).

```bash
bash scripts/query-tuning/seed-scale.sh local-mysql-x10  10
bash scripts/query-tuning/seed-scale.sh local-mysql-x100 100
# x1 소스가 다른 컨테이너면 3번째 인자로 지정 (기본 local-mysql-x1, 정지 상태면 자동 기동·원복)
```

각 실행 끝에:
- **충분성 가드**(`seed-scale.sh` 내장 `check_x1_scale`)가 자동 실행 — x1×100 이 **10K 미만인 도메인 테이블을 경고**(진행은 안 막음).
  새 도메인 테이블이 생겨도 information_schema 자동 발견으로 검사된다. §2 의 x1 베이스 top-up(100행) 덕에 소형 4개는 경고를 안 낸다.
- **총량 검증표**(원본 × 배수) 출력 — `board`·`feedback_board`·`quiz_folder`·`reply` 는 base 100 → x100 에서 10,000.

---

## 4. 스케일 스윕 + 요청 귀속 → 대시보드 §①②③

앱을 각 레벨 DB에 붙여 부하를 태우고 요청을 SQL 주석으로 귀속(`trace_snapshot`)한다.

```bash
ROUNDS=50 bash scripts/query-tuning/run.sh          # 전 레벨 순차
# 또는 특정 레벨만
bash scripts/query-tuning/run.sh x100
```

- `run.sh` 이 스윕 전 bootJar 1회 빌드 → 레벨별 `run_level`(내장 () 서브셸 함수) 실행. 중간 레벨(x1·x10)은 끝나면 정지(RAM 반환), **x100(3309)만 상주**(대시보드가 읽음).
- `run_level` 은 **실데이터·볼륨을 안 건드린다** — `trace_snapshot`·perf_schema·slow_log 만 이번 실행분으로 갱신. 부하는 내장 `loadgen` 함수가 실 엔드포인트를 태운다.
- 개별 레벨 실행(옛 `run-level.sh` 직접 실행 대체):
  ```bash
  bash scripts/query-tuning/run.sh x1
  bash scripts/query-tuning/run.sh x10
  bash scripts/query-tuning/run.sh x100
  ```

---

## 5. 대시보드 확인

- **[쿼리튜닝] 통합 진단** (uid `q-asker-unified-diagnosis`) — http://localhost:3000/d/q-asker-unified-diagnosis
- 좌상단 `레포지토리` 변수로 대상 레포 선택.
  - **§①**(Prometheus): 스케일별 레포·메서드 지연 — 크기 의존 O(n) 탐지.
  - **§②③**(MySQL `trace_snapshot`): 엔드포인트별 요청당 쿼리수(N+1)·스캔행수·대표 SQL 시퀀스.
  - **§④**(Prometheus): 레포별 읽기/쓰기 비중. **부록**: slow_log.
- §②③이 "no data" 면 §4 스윕이 안 돌았거나 MySQL 데이터소스가 x100(3309)를 못 봄 → 아래 트러블슈팅.

---

## 재시딩 / 배수 변경

배수를 바꾸거나 데이터를 초기화하려면 **3단계 seed-scale 를 다시 실행**하면 된다(초기화 포함이라 그대로 재실행 안전).

```bash
bash scripts/query-tuning/seed-scale.sh local-mysql-x100 200   # x200 으로 재현
```

> "x100 을 다시 돌리면 x200 이 되나?" → **아니다.** seed-scale 은 매번 볼륨 재생성+복원부터 하므로 항상 fresh x\<배수\>다.
> 재실행 누적·중복키 걱정 없음.

---

## 트러블슈팅

| 증상 | 원인 / 조치 |
|---|---|
| `run.sh`(run_level): "컨테이너 없음" | provision(2) + seed(3) 를 먼저. 컨테이너를 자동 생성하지 않음(실수 방지). |
| `seed-scale`: "x1 소스 없음" | 1단계(provision) + 2단계(`restore-x1.sh`) 로 `local-mysql-x1` 먼저 채움. |
| `seed-scale`: "x1 을 대상으로 줄 수 없음" | 대상에 `local-mysql-x1` 을 넣음 — 원본 보호로 거부. x10/x100 을 대상으로. |
| 대시보드 §②③ no data | (a) 해당 레벨 `run.sh` 이 안 돌아 `trace_snapshot` 없음, (b) x100 컨테이너 정지, (c) 데이터소스 헬스: `curl -s -u admin:inha-cc-01 localhost:3000/api/datasources/uid/bfrwvbnu3162ob/health`. |
| Grafana 로그인 실패 | `docker exec local-grafana grafana cli admin reset-admin-password inha-cc-01`. |
| stale `local-mysqld-exporter-*` | 옛 이름 익스포터 잔재 — `run.sh`(run_level)이 재생성하거나 `docker rm -f` 로 정리. |
| 충분성 경고(10K 미만) | 정보성 경고(진행 무관, x1×배수 투영 기준). `restore-x1.sh` 의 x1 베이스 top-up(`seed-x1-base.sql`, 소형 테이블 100행)이 돌았으면 경고가 안 뜬다. 안 떴다면 x1 재복원 필요. |
| RAM 압박 | 레벨 동시 상주 금지. run.sh 이 x1·x10 을 자동 정지. 수동이면 `docker stop`. |

---

## 정리 (측정 종료 후)

```bash
docker stop local-mysql-x1 local-mysql-x10          # x100 은 대시보드용으로 유지 가능
# 완전 제거(볼륨은 남음): docker rm -f local-mysql-x1 local-mysql-x10 local-mysql-x100
# 볼륨까지: docker volume ls | grep <해시> 확인 후 docker volume rm
```
